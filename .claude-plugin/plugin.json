{
  "name": "the-advisor",
  "description": "Second-opinion layer for Claude Code: a cheap workhorse model is obligated to consult a stronger advisor model before architectural or hard-to-reverse decisions. The advisor can be any provider — Claude, Gemini, OpenAI, or a local model via any OpenAI-compatible endpoint — chosen by config, not code.",
  "version": "0.4.0",
  "author": {
    "name": "drpakfro"
  },
  "homepage": "https://github.com/alitahir6001/the-advisor",
  "license": "MIT",
  "keywords": ["advisor", "mcp", "multi-model", "workhorse", "pattern", "code-review", "second-opinion"],
  "userConfig": {
    "advisor_model": {
      "type": "string",
      "title": "Advisor model",
      "required": true,
      "description": "The strongest model you have access to. Must be a name your provider accepts — see example model-name links in the README. Set it to cli-default only if you deliberately want your CLI's own default model.",
      "default": ""
    },
    "advisor_provider": {
      "type": "string",
      "title": "Advisor provider",
      "description": "Auto-detected from the model name if left blank: claude-* uses your Claude CLI, gemini-* uses Gemini CLI, gpt-* uses the OpenAI API. Set advisor_base_url for local/gateway models. Override here only for edge cases.",
      "default": ""
    },
    "advisor_base_url": {
      "type": "string",
      "title": "Advisor base URL",
      "description": "Only for local models or gateways, e.g. http://localhost:11434/v1 for ollama. Leave blank for Claude, Gemini, or OpenAI.",
      "default": ""
    },
    "advisor_api_key": {
      "type": "string",
      "title": "Advisor API key",
      "sensitive": true,
      "description": "Only for the *-api providers. Leave blank to use ANTHROPIC_API_KEY / OPENAI_API_KEY from your shell instead.",
      "default": ""
    },
    "python_command": {
      "type": "string",
      "title": "Python command",
      "description": "How python3 is invoked on your system. Leave as python3 on macOS and Linux. On Windows set this to python or py — python3 does not exist there.",
      "default": "python3"
    }
  }
}
